import catchError from './utils/catchError';
import isError from './utils/isError';
import isErrorReason from './utils/isErrorReason';
import isErrorReasonRule from './utils/isErrorReasonRule';

export const yumUtils = {
  catchError,
  isError,
  isErrorReason,
  isErrorReasonRule,
};
